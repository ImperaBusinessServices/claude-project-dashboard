## What's new in v2.9.1

**💸 Pick several projects at once.** The Spend section's project filter is now a checkbox dropdown — tick any mix of projects and every number on screen follows. Clicking rows in the By-project table adds/removes them too (selected rows highlight).

**Whole dollars in the tables.** The By-project and By-model tables now round to the nearest dollar — cleaner to read.

---

Both installers are attached below:
- **Windows:** `Claude.Project.Dashboard.Setup.2.9.1.exe` — SmartScreen may show "Windows protected your PC"; click **More info → Run anyway** (the app isn't code-signed on Windows; that's normal).
- **Mac:** `Claude.Project.Dashboard-2.9.1-universal.dmg` — Apple-signed + notarized; opens with no warnings. Works on Apple Silicon and Intel.

Or just grab it from **https://claude-manager.com** — it always serves the newest version.
